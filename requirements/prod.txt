# Linux 生产机（NVIDIA CUDA，Python 3.10）—— install.sh 装的就是这份。
#
# torch / torchvision 不在这里：install.sh 在本文件之前已从源机 wheelhouse 离线装好
# （那个目录本身就是钉版，版本见 build.sh 开头）。

-r base.txt

# ultralytics 会把 numpy 拉到 2.x，与钉版的 torch wheel 不兼容，必须钉回 1.26.4。
# install 脚本在末尾还会再钉一次（装 ultralytics 时可能被顶上去）。
numpy==1.26.4
