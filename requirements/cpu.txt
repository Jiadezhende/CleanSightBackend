# CPU-only 机器（无 GPU 的开发/验证机）—— PyTorch 走 CPU wheel 源，体积远小于 CUDA 版。
#
# 用法：pip install -r requirements/cpu.txt
# 与生产主线（cuda.txt）的唯一差别就是 torch 从哪来。

--extra-index-url https://download.pytorch.org/whl/cpu

-r base.txt

# 版本与 deploy.conf 的 TORCH_PKGS 对齐，只是换了 CPU 轮子。
torch==2.8.0
torchvision==0.23.0

numpy==1.26.4
