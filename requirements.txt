# 本文件是唯一依赖源，供 install.sh 使用。
# torch / torchvision 不在此列——它们是核心静态物料，版本钉在 deploy.conf，
# 由 build.sh 下进 wheelhouse、install.sh 离线装。

# ========== Core AI / CUDA ==========
# torch / torchvision 见 deploy.conf（TORCH_PKGS）
ultralytics~=8.3.200
lap>=0.5.12

# ========== Numerical / CV ==========
numpy==1.26.4
opencv-python-headless<4.12.0
pillow>=10.4,<12.0
# ultralytics会导入opencv-python，与headless冲突
# 所以还需要手动清理一下opencv-python

# ========== Web Framework ==========
fastapi==0.121.2
uvicorn[standard]==0.38.0
python-multipart>=0.0.20
websockets>=13,<16

# ========== HTTP Clients ==========
requests>=2.31,<3.0
httpx>=0.27,<0.29

# ========== Database ==========
sqlalchemy==2.0.44
psycopg2-binary==2.9.11
pydantic-settings==2.12.0

# ========== Testing (dev only) ==========
pytest>=7.4,<9.0
pytest-asyncio>=0.23,<1.4
pytest-cov>=4.1  # 覆盖率量化：pytest tests/ --cov=app --cov-report=term-missing

# ========== 日志 ==========
colorlog==6.9.0
concurrent-log-handler==0.9.29
prometheus_client==0.16.0