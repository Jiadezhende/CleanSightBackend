# 变更记录填写模板

> 复制本文件正文（从下方 `---` 之后）另存为新变更记录。本说明部分不要保留。

**命名**：变更记录统一放在 `docs/update/`，按 `日期_大写关键词.md` 命名，例如 `20260616_DOWNGRADE_TO_YOLO.md`、`20260613_ENV_ISOLATE.md`。

**章节顺序按读者认知规律固定**：元数据 → 概述 → 变更背景 → 方案详情 → 变更效果 → 遗留风险 / 后续任务。读者先知道「这篇讲什么」，再知道「为什么要改」，才有兴趣读「怎么改的」，最后确认「改完值不值、还欠什么」。别打乱这个顺序。

**先有全景意识，再谈细节**：`方案详情` 的第一件事是给出**全景**——整条流程 / 数据怎么流 / 改动分几块，一段伪代码或一张图说完，紧跟一张「步骤（或部件）→ 落在哪个模块 → 详见第几节」的对应表。后面每个小节都是全景里某一步的实现细节，**编号严格按全景顺序**，读者能把每节挂回主干。没有全景就先讲数据契约、先讲某处重构，等于让人在看不见链路的情况下评审局部——判断不了这个契约服务于哪一步，也看不出哪里缺了。改动本身没有流程形态（如纯配置调整）时，全景退化为「改了哪几块 + 彼此关系」一句话，但不能没有。

**篇幅自适应**：小改动只需保留「标题 + 元数据 + 概述」三段即可；涉及多文件/配置/回退的改造再按需补全后面的小节，没用到的整段删除，不要留空壳。

**别搞「总纲 + 引用」**：每篇变更记录是**能独立读完的任务单元**，把自己的契约/前提/背景写进本文，不要把它甩给一篇「总纲/hub」文档、让读者非跳过去不可（`契约前置：X`、`取自定稿见 X`、正文靠 `见 X §Y` 才成立，都属此病）。任务确实可能依赖之前的重构——那就用一句话点明依赖关系（「本次建立在 XX 换键之上」），但不把关键内容留在别处。可正常链接**代码**（`../../app/...`）、**对外 API/wire 文档**、**知识库**（`../kb/...`）——这些是真源，不是平级变更记录。

**两条状态轴（务必都填）**：

- **变更状态** = 这次改动的技术生命周期，决定它该不该进知识库。`已回退` 不仅不沉淀，还应触发清理 kb 里对应的旧结论。
- **知识库** = 这条记录有没有被聚合进 `docs/kb/`。新写的记录默认 `待沉淀`；定期维护时把已生效内容沉淀进 kb，再回填 `已沉淀 → 目标kb文件(日期)`，此后该记录变为纯历史，聚合自动跳过。

定期聚合时，先用一行命令捞出欠债：

```bash
grep -l '待沉淀' docs/update/*.md          # 列出所有尚未进 kb 的变更记录
```

仅把「变更状态 = 生效中」的内容写入 kb；遇到 `已回退`，反向检查并删除 kb 中失效结论。沉淀后务必更新目标 kb 文件顶部的「更新时间」（见 [kb/KB_MAINTENANCE.md](../kb/KB_MAINTENANCE.md)）。

---

`````markdown
# {改造内容核心，一句话点题}

> **变更状态**：生效中（YYYY-MM-DD）　<!-- 生效中 / 进行中 / 已回退 / 提案；一句话说明当前系统实际处于什么状态 -->
> **知识库**：待沉淀　<!-- 待沉淀 / 已沉淀 → [目标kb文件](../kb/XXX.md)(YYYY-MM-DD) / 无需沉淀(临时性变更，原因) -->
>
> <!-- 元数据块只放状态轴。背景、依赖、真源链接都写进下方「变更背景」，别在这里铺开。 -->

## 概述

> **100 字以内**，只回答「这篇文档讲了什么变更」：改了什么 + 影响面。不展开原因和实现——那是后面两节的事。读者靠这段决定要不要继续读。

## 变更背景

> 给读者读懂后文所需的**必要上下文**：改之前系统是什么样、遇到了什么问题、触发这次改动的是什么（需求 / 评审阻断项 / 线上问题 / 重构依赖）。写「为什么必须改」，不写「怎么改」。

- **现状 / 痛点**：……
- **触发来源**：……（可链接 [specs/XXX.md](../../specs/XXX.md) 评审、对外 API 文档、代码真源）
- **承接**：一句话点明依赖的前序重构（若有），关键内容仍写在本文，不让读者跳转别处取。

> 阻断项 / 问题较多时用表格列出：
>
> | 编号 | 问题 | 风险 |
> |------|------|------|
> | #1 | …… | 上线阻断 / 高 / 潜在正确性 |

## 方案详情

> 顺序固定：**全景 → 决策 → 实现**。先让读者看见整条链路长什么样，再讲为什么选这个方案（备选是什么、为何否掉），最后逐个改动点展开。每个改动点标注**文件路径**，必要时给「旧 → 新」对照。可点击的代码引用用 [`符号名`](../../app/path/file.py) 形式。

### 全景：{主干流程 / 改动版图}

> **必填，且必须排在本节最前。** 一段伪代码、一张 ASCII 图或一条调用链，把「数据从哪来、经过哪几步、落到哪」说完；步骤之间的**硬约束**（谁必须先于谁、为什么不能换序）也写在这里，它属于主干而非细节。

```text
{步骤 1} → {步骤 2} → {步骤 3}
```

> 紧跟对应表，把主干挂到下面各节；后面的实现小节**不重复贴这段伪代码**，只写该步自己的约束与取舍。

| 步骤 / 部件 | 落在哪 | 详见 |
|------------|--------|------|
| …… | `path/to/file` | §1 |

### 方案选型

| 方案 | 代价 / 影响面 | 结论 |
|------|--------------|------|
| A（采用） | …… | 选它的理由 |
| B | …… | 否掉的理由 |

### 1. `path/to/file` — 一句话说明改动意图（对应全景第 X 步）

#### 旧
```
…变更前关键片段…
```

#### 新
```
…变更后关键片段…
```

> 关键约定 / 易踩坑点用引用块补充说明。

### 2. 保留项（不改动）

- 明确列出**刻意保留**的代码/配置，避免后人误删或误以为遗漏。

### 数据通道 / 行为说明（可选）

> 当改动影响数据流向、队列、开关语义时，用表格澄清「谁写、谁读、本次开关是否影响」，消除误解。

| 通道 | 填充 | 消费 | 本次影响 |
|------|------|------|---------|
| …… | …… | …… | 是 / 否 |

## 变更效果

> 对照「变更背景」里的痛点，说明**达成了什么**。能量化就给数字（前 → 后），不能量化就写行为差异。自测结果一并附在这里——它是效果的证据，不是独立话题。

| 维度 | 变更前 | 变更后 |
|------|--------|--------|
| …… | …… | …… |

**自测结果**

| 项 | 结果 |
|----|------|
| 相关单测 | N passed |
| 配置加载 / 启动自检 | …… |
| 全量 `pytest tests/` | …… passed；如有 failed 注明是否为预存/环境问题，与本改动无关 |

## 遗留风险 / 后续任务

> 诚实列出**没做完的**和**可能出问题的**：已知未覆盖场景、埋下的技术债、需要观察的指标。回退类或分期落地的改动，在这里给复原步骤或下一期待办，尤其标注「恢复前必须先修的阻断项」。没有遗留就写「无」，别留空壳。

| 风险 / 待办 | 影响 | 处理计划 |
|------------|------|---------|
| …… | …… | …… |
`````
