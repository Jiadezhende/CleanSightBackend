# CleanSightBackend 依赖清单 —— 阿里 PPU-ZW810E 平台（非 NVIDIA）
#
# 适用机器：CUDA 兼容层为 /usr/local/PPU_SDK/CUDA_SDK 的阿里 PPU 实例。
# 判别方式：nvidia-smi 输出头部是 "PPU-SMI" 而非 "NVIDIA-SMI"，设备节点为 /dev/alixpu_ppu*。
# 主线 requirements.txt / install.sh（cp310 + 官方 CUDA wheel）在此类机器上不可用。
#
# 用法（venv 必须带 --system-site-packages，否则 PPU torch 不可见）：
#   source /usr/local/PPU_SDK/envsetup.sh          # 缺此步 PPU 内核编译会 abort
#   /usr/local/bin/python3 -m venv --system-site-packages .venv
#   .venv/bin/pip install -i https://mirrors.aliyun.com/pypi/simple/ -r requirements-ppu.txt
#
# 【刻意不列】以下由系统 site-packages 提供，装进 venv 会 shadow 掉厂商定制版、PPU 立刻不可用：
#   torch 2.10.0+ppu2.1.0 / torchvision 0.25.0+ppu2.1.0 / torchaudio 2.10.0+ppu2.1.0
#   numpy 2.3.5  —— PPU torch 按 numpy 2.x 编译，绝不可按主线 requirements.txt 降到 1.26.4
#
# 与主线 requirements.txt 的差异仅上述两处，其余版本约束完全对齐。
# 验证记录见 docs/update/20260912_PPU_PLATFORM_DEPS.md。

# ========== Core AI ==========
ultralytics~=8.3.200
lap>=0.5.12

# ========== CV ==========
# 必须显式装：系统那份 cv2 是 QT5 GUI 版(opencv-python 4.11.0.86)，
# 服务端无显示环境，需用 headless 在 venv 内 shadow 掉它。
opencv-python-headless<4.12.0
pillow>=10.4,<12.0

# ========== Web Framework ==========
fastapi==0.121.2
uvicorn[standard]==0.38.0
python-multipart>=0.0.20
websockets>=13,<16

# ========== HTTP Clients ==========
requests>=2.31,<3.0
httpx>=0.27,<0.29

# ========== Database ==========
sqlalchemy==2.0.44
psycopg2-binary==2.9.11
pydantic-settings==2.12.0

# ========== Testing (dev only) ==========
pytest>=7.4,<9.0
pytest-asyncio>=0.23,<1.4
pytest-cov>=4.1

# ========== 日志 ==========
colorlog==6.9.0
concurrent-log-handler==0.9.29
prometheus_client==0.16.0
